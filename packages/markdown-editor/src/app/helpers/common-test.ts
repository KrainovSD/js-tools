export const COMMON_TEST = `# Заголовок для якорей

## Второй заголовок

## Дубликат

## Дубликат

## Дубликат

1. [[вики-ссылка]] — обычный клик выделяет текст, Ctrl+клик ведёт к заголовку с таким текстом, а если его нет — открывает ссылку в новом окне (пробелы в ссылках не поддерживаются, как в обычном markdown)
2. ^^подчёркнутый текст^^ и ^^многострочный
   текст^^ — подчёркивание пересекает строки, как зачёркивание
3. Якоря (слаг заголовка, пробелы = дефисы): [[#второй-заголовок]] и [текст](#заголовок-для-якорей) — Ctrl+клик ведёт к разделу; у одинаковых заголовков id с суффиксом: [[#дубликат]] — к первому, [[#дубликат-1]] — ко второму
4. ~~зачёркнутый~~
5. Удалите «2» у второго пункта — номера пересчитаются:
   1. первый
   2. второй
   3. третий
6. Чекбокс: Mod-Shift-x на строке
   - [ ] задача
   - [x] сделанная задача

@7zn2FxaHyG
@test
test

![test](https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcROHxYft1f_Ln_y_scKnh8-g5rLMmce7JKyPQ&s)

\`\`\`js

const string = "string"
const number = 20
const bool = true
const arrray = [1, 2, 3]
\`\`\`

\`\`\`yaml
redis:
  out:
    - landing_availability
    - name: landing_availability_set
      entities:
        - landings
      comment: Кладет список лендингов

\`\`\`

## test
`;
